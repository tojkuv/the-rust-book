fn main() {







}

// note: `_` is the catch all pattern that will never bind to a value

// note: patterns in if statements allow for more flexibility; they behave similar to a single
// arm in match expressions.

// note: patterns can be used in `while` loop expressions that compares a value to the given
// pattern

